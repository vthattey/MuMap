import React from "react";
import ReactDOM from "react-dom/client";
import StoryboardMural from "./StoryboardMural.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StoryboardMural />
  </React.StrictMode>
);
